#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 * @x: first integer
 * @y: second integer
 *
 * Return: sum of both integers
 */
int add(int x, int y)
{
	return (x + y);
}
